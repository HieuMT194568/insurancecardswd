# Copy file này thành application-local.yml (cùng thư mục) rồi điền thông tin máy bạn.
# application-local.yml nằm trong .gitignore nên không bị đẩy lên git.
# Biến môi trường cùng tên (DB_PASSWORD...) vẫn được ưu tiên hơn file này.

DB_USERNAME: root
DB_PASSWORD: your_mysql_password
# DB_URL: jdbc:mysql://localhost:3306/insurance_card?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&connectionTimeZone=Asia/Ho_Chi_Minh
# DB_VENDOR: sqlserver
